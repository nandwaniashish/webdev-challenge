Event(s) tracked: `onChange`, `onBlur`

### Checkbox

```jsx
const onChange = (event)=> {
  //manage state using event.target.value
}

<Input id="input" label="this is an excercise" onChange={onChange} aria-describedby="alert">
<div id="alert"> Errorrrrrrr </div>
```
